query GetUser($getUserId: String!) {
  getUser(id: $getUserId) {
    id
    name
    imageUrl
    posts {
      id
      caption
      createdAt
      imageUrl
    }
  }
}

mutation Logout {
  logout {
    message
  }
}
